# Expanded homepage — v0.10.0

The homepage now introduces the full ChartsAI library through three direct choices: make a chart, explore math tools, or find a printable. The user selected the Printed Resource Library design from four visual mockups. Blue/pink printed ink and paper textures are scoped to the homepage; existing editor workbenches retain their established interface.

## Useful discovery

The static page lists all eight chart makers, five math tools and both printable builders. It links the 50 original worked examples, 20 guides, five attributed UCI datasets, methods and public source. A truthful ItemList describes the 15 visible tools. Existing canonical URLs and 109 sitemap routes remain unchanged; this release adds no keyword permutations or indexable pages.

The opening shows real native ECharts and shared number-line outputs, with explicitly fictional chart data. The monthly series match the linked example, all 48 scores remain represented, and the slope axes have equal physical units. Generated textures are background materials only. The previous interactive chart-style preview and exact example/theme handoff remain available below the directory, without loading ECharts in the homepage browser.

## Accessibility and privacy

Primary navigation and the complete directory work without JavaScript. Mobile stacks the three paths before the previews. Keyboard focus connects a category to its corresponding preview, with a still reduced-motion state. The headline remains selectable text; forced-color mode uses the system text color. Files and custom data remain browser-local. Existing bounded Plausible measurement is unchanged.

## Verification

Local checks cover 120 application unit tests and six projection-checker fixtures, static metadata/schema/resources for 110 HTML pages and 109 sitemap routes, and 13 relevant browser flows including imports, real exports, exact example/style handoff, phone layouts and no-JavaScript discovery. Additional material-rebuild runs recheck the affected homepage, presentation and data-integrity behavior. The Impeccable review evidence and generated mockup decisions stay local; durable design conventions live in DESIGN.md.

A production release must separately verify the deployment's Ready status, custom aliases, current homepage content, asset delivery, sitemap and representative user flows. Passing local checks does not establish search visibility, learning outcomes or Core Web Vitals field performance.
